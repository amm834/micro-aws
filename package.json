{
  "name": "micro-aws",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "packageManager": "^pnpm@7.20.0",
  "scripts": {
    "dev": "pnpm  --parallel --filter \"./**\" dev",
    "build": "pnpm  --parallel --filter \"./**\" build",
    "preview": "pnpm  --parallel --filter \"./**\" preview",
    "stop": "kill-port --port 3001,3002"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kill-port": "^2.0.1"
  }
}
